disabled_rules:
  - trailing_whitespace

excluded:
  - .build
  - fastlane/test_output

opt_in_rules:
  - empty_count
  - explicit_init
  - force_unwrapping

line_length:
  warning: 120
  error: 160

function_body_length:
  warning: 50
  error: 100
